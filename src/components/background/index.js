import { Box } from "@mui/material";

const Background = () => <Box sx={{
    backgroundColor: '#b5ffbb',
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: '100%',
    zIndex: -1,
    opacity: 0.8
}}/>;

export default Background;
